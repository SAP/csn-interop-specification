{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "cmd=$(jq -r '.tool_input.command // \"\"'); if echo \"$cmd\" | grep -qE '^(gh pr (create|edit)|git commit -m)'; then if echo \"$cmd\" | grep -qoE 'github\\.tools\\.sap|jira\\.sap\\.com|wiki\\.wdf\\.sap\\.corp|sap\\.corp'; then match=$(echo \"$cmd\" | grep -oE 'github\\.tools\\.sap|jira\\.sap\\.com|wiki\\.wdf\\.sap\\.corp|sap\\.corp' | head -1); echo \"BLOCKED: internal reference \\\"$match\\\" in command targeting a PUBLIC repo. Scrub before retrying.\" >&2; exit 2; fi; fi; exit 0"
          }
        ]
      }
    ]
  }
}
